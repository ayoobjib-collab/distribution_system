

function Index({ h1, transactions, clientId, msg, financialSummary }) {

    return (
        <>

        </>
    )
}

Index.layout = page => <DashboardLayout children={page} h1={page.props.h1} />

export default Index;